from setuptools import setup

setup(
    name='Thread Hunt',
    version='1.0',
    description='A duck hunt that kills your processes',
    author='Will McCall',
    author_email='will@willmccall.dev',
    install_requires=['wxpython'],  # external packages as dependencies
)
